import styles from "./Navbar.module.css";

const Navbar = () => {
  return (
    <div className={styles.navbar}>
      <h3 className={styles.title}>Cloudmart</h3>
    </div>
  );
}

export default Navbar;
